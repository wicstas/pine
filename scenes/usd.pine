scene := Scene();
image_size := [640, 640];

scene.load("scenes/model/kitchen/kitchen.glb");
#scene.set(ImageSky("scenes/hdr/rural.jpg"));
#scene.add(DirectionalLight([-2, 1, 0.4], [10, 10, 10]));
scene.add(HomoMedium(AABB([-20, -10, -20], [20, 10, 20]), HgPF(), 0.0, 0.05));

PathIntegrator(BlueSampler(1), 5).render(scene);
scene.camera.film().save("images/usd.png");
